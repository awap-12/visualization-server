{
    "name": "server",
    "version": "1.0.0",
    "workspaces": [
        "artifacts/*"
    ],
    "dependencies": {
        "sequelize": "latest",
        "server": "file:."
    },
    "devDependencies": {
        "cross-env": "latest",
        "mocha": "latest"
    },
    "scripts": {
        "test": "cross-env NODE_ENV=development mocha artifacts/**/test/*.js --exit"
    }
}
